// This service has been disabled to remove costs and dependencies.
// All announcement drafting is now manual.
export const geminiService = {
  draftAnnouncement: async () => {
    return { title: "", content: "" };
  }
};
